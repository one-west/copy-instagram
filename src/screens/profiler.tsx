import styled from "styled-components"

const Container = styled.div``;
const Title = styled.h1``;

export default () => {
    return <Container>
        <Title>Profiler Page.</Title>
    </Container>
}

// 참조해서 